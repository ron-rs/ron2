Schema(
    doc: "Application configuration for a web server.",
    kind: Struct(
        fields: [
            Field(
                name: "port",
                ty: U16,
                doc: "Server port number."
            ),
            Field(
                name: "host",
                ty: String,
                doc: "Server hostname.",
                optional: true
            ),
            Field(
                name: "debug",
                ty: Bool,
                doc: "Whether debug mode is enabled.",
                optional: true
            ),
            Field(
                name: "features",
                ty: List(
                    TypeRef(
                        "Feature"
                    )
                ),
                doc: "List of enabled features."
            ),
            Field(
                name: "database",
                ty: TypeRef(
                    "DatabaseConfig"
                ),
                doc: "Database configuration."
            )
        ]
    )
)